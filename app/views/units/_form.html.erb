<%= simple_form_for(unit) do |f| %>
  <%= f.input :name%>
  <%= f.input :photo %>
  <%= f.input :category %>
  <%= f.input :description %>
  <%= f.submit "Create unit", class: "btn btn-primary" %>
<% end %>
