
<div class="unit_form">
<%= simple_form_for(unit) do |f| %>
  <%= f.input :name %>
  <%= f.input :photo %>
  <%= f.input :category %>
  <%= f.input :description %>
  <%= f.submit "Create Unit", class: "btn btn-outline-light" %>
<% end %>
</div>
